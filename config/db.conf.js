const protocol = "";
const login = "";
const mdp = "";
const url = "";
const name = "";
const connectUrl = `${protocol}://${login}:${mdp}@${url}/${name}`;

module.exports = { protocol, login, mdp, url, name, connectUrl };